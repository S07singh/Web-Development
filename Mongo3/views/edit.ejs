<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chat</title>
</head>
<body>
    <p>Edit your chat: <%= chat._id %></p>
    <p>This chat was sent form <b><%= chat.sender %> </b> to <b> <%= chat.receiver %> </b></p>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="message" cols="50" rows="6" placeholder="Type your message here..."></textarea>
        <button>Edit</button>
    </form>
</body>
</html>